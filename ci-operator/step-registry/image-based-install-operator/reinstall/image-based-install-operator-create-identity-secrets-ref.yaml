ref:
  as: image-based-install-operator-create-identity-secrets
  from: src
  commands: image-based-install-operator-create-identity-secrets.sh
  resources:
    requests:
      cpu: 100m
      memory: 50Mi
  documentation: |-
    Create secrets containing cluster identity
